type: object
required:
  - email
  - oldPassword
  - newPassword
properties:
  email:
    description: User's email
    type: string
    format: email
    minLength: 1
    maxLength: 45
  oldPassword:
    description: User's old password
    type: string
    format: password
    minLength: 5
    maxLength: 45
  newPassword:
    description: User's new password
    type: string
    format: password
    minLength: 5
    maxLength: 45
